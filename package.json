{
  "name": "linuxdospace",
  "version": "0.1.0-alpha.2",
  "description": "LinuxDoSpace Node.js SDK (TypeScript) for HTTPS mail stream",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"import('node:fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\"",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "mailparser": "^3.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "typescript": "^5.9.2"
  }
}
